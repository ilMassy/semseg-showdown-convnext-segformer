# Core deep learning
torch>=2.2.0
torchvision>=0.17.0

# Modelli Transformer (SegFormer) pretrained
transformers>=4.40.0

# Encoder moderni (ConvNeXt, ecc.) + architetture segmentazione pronte
segmentation-models-pytorch>=0.3.3
timm>=0.9.16

# Data augmentation per segmentazione
albumentations>=1.4.0

# Metriche di segmentazione (mIoU, Dice, ecc.)
torchmetrics>=1.3.0

# Experiment tracking (free tier accademico)
wandb>=0.16.0

# Explainability
grad-cam>=1.5.0

# Utility
numpy
matplotlib
opencv-python
tqdm
pyyaml

# Deploy (fase successiva)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart
